<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Лаб 2-3</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Главная<span class="sr-only">(current)</span></a></li>
        <li><%= link_to "Пользователи", users_path%></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="developer">Разработчик</a></li>
        <% if !(user_signed_in?) %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Авторизация<span class="caret"></span></a>
            <ul class="dropdown-menu col-md-4" id = "sign_in_menu">
            <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <li><%= f.input :email, required: false, autofocus: true %></li>
              <li><%= f.input :password, required: false, label: "Пароль" %></li>
              <li style="margin-left: 2em"><%= f.input :remember_me, as: :boolean, label: "Запомнить меня" if devise_mapping.rememberable? %></li>
              <li><%= f.button :submit, "Войти" %><a href=" <%=new_registration_path(resource_name)%> " style="margin-left: 1em;"> Регистрация </a></li>
            <% end %>
            </ul>
          </li>
        <%else%>
          <li><%= link_to current_user.email, user_path(current_user)%></li>
          <li><%= link_to "Выйти", destroy_user_session_path, :method => :delete %></li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

